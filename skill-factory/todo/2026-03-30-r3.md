# /skx Report — 2026-03-30 (Run 3)

## Vault Signals
- Notes: 49 | Ideas: 8 | Active TODOs: 77
- Tags without skills: `monitoring`(5), `roadmap`(4), `docker`(2), `ci-cd`(2), `weekly-review`(2)
- Recent focus themes: DSPy 試點、運營系統升級路線圖、Remotion 影片生成

## Git Patterns
- TG Bot: GA4 auth 失敗 3+ commits (ga4-alert, credential detection, fix auth broken) — 重複修復
- clausidian: `release:` + `chore: bump version` 各 2x → version-sync 已覆蓋
- GWX: `feat(skill):` 4x in 20 → skill DSL 開發密集

## Already Built (跳過)
pypi-publish, agent-trace, site-doctor, prompt-audit, launchd-health,
auto-daily, vault-registry, golden-add, gwx-health, competitive, ctx

---

## Skill Ideas

### P1 — High Signal (build this week)
| # | Skill Name | Trigger | Description | Tools |
|---|-----------|---------|-------------|-------|
| 1 | `/dspy` | "optimize prompt", "DSPy 試點", "tag 準確率" | 用 DSPy 自動優化 Clausidian tag matching prompt，輸出 A/B 結果 | Bash, Read, Agent |

**Why P1**: vault idea `dspy-trial-plan` (active status) + agenda TODO "DSPy 試點：Obsidian tag 匹配準確率自動優化" — 明確需求 + 具體場景(clausidian auto-tag) + 無現有 skill

### P2 — Medium Signal (build next)
| # | Skill Name | Trigger | Description | Tools |
|---|-----------|---------|-------------|-------|
| 2 | `/ga4-health` | "GA4 auth", "GA4 broken", "GA4 診斷" | 診斷 GA4 API 憑證狀態 + 最近失敗查詢，輸出修復建議 | Bash, Read |
| 3 | `/ops-status` | "升級路線圖", "phase 進度", "ops roadmap" | 讀取 ops-system-upgrade-roadmap vault note + git，輸出 4 phase 當前進度 | Bash, Read, Agent |
| 4 | `/remotion` | "remotion clip", "影片剪輯", "生成影片" | Remotion React 影片生成腳手架 — 給定腳本+資產輸出 mp4 | Bash, Agent |

**Why P2**:
- `/ga4-health`: 3+ 重複 GA4 auth fix commits in TG Bot — 症狀反復 → 需要診斷工具
- `/ops-status`: active vault project + `roadmap` tag x4 — 無進度追蹤 skill
- `/remotion`: vault project `remotion-clip` (draft) — `sites` 運營需要影片素材生成

### P3 — Low Signal (backlog)
- `/dashboard-validate` — session-wrap 儀表板 ROI 觀察模板 (vault idea draft, low urgency)
- `/docker-health` — docker tag x2, sub2api-deploy project (too narrow currently)

---

## Quick Wins (< 30 min)
- `/ga4-health` → 15-20 行：檢查 ~/.config/gwx/ga4-credentials.json + 試打一個 API call
- `/ops-status` → 讀 vault note + `git log --oneline -10` across 3 projects → 彙整輸出

## Gaps in Tool Chain
- TG Bot ↔ GA4: 3+ fixes 但無診斷工具 — 每次靠 git blame 定位
- Remotion ↔ Claude Code: 影片生成完全手動，無 skill 腳手架
- DSPy ↔ Clausidian: tag 優化需要 DSPy 但無連接層
